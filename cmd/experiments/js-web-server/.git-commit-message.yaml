title: "🔧 Add dynamic port allocation to js-web-server MCP"
description: |
  - Created WebServerMCP struct to manage server state and port allocation
  - Implemented findFreePort() function to find available ports starting from 8080
  - Updated MCP tool registration to include correct server URLs in description
  - Modified all engine references to use new struct-based approach
  - Server URLs in tool responses now reflect actual running port
  
  Tests run:
  - go build: PASSED
  - go fmt: PASSED
